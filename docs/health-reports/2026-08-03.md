# FXcrypt1 Daily Health Report — 2026-08-03

## Summary

**Overall health: GREEN.** No commits in the last 26 hours, no secrets found, firestore.rules is sound, all syntax/build checks pass, and only one actionable dependency finding.

Top findings, ranked:

1. **(Medium)** `webapp/` has 2 **high**-severity npm advisories: `next@14.2.35` (multiple CVEs — DoS, cache poisoning, SSRF, XSS) and its transitive `postcss` (XSS / arbitrary file read via sourceMappingURL). Fix requires a semver-major bump to Next 16.x — needs a scoped upgrade + regression pass, not a blind `audit fix --force`.
2. **(Low)** `functions/` has 11 **moderate** npm advisories (no high/critical) — no action required per this run's report threshold, listed for awareness only.
3. **(Informational)** No stale TODO/FIXME/HACK comments found in application code. Docs are current (`ROADMAP.md` self-flags as superseded; `PRODUCT_STRATEGY.md` §0 checked against code and matches).

No cross-file contract breaks, no exposed secrets, no permissive Firestore rules, no build/syntax failures.

---

## 1. Recent-Commit Review

`git log --since="26 hours ago" --stat` returned **no commits**. The most recent commit on `main` is `856724d` ("Scrub leaked Telegram bot token from settings.json", 2026-07-31 00:17 +01:00), which is outside the 26-hour window. Nothing to review for this run.

---

## 2. Security Scan

### Secrets
Grepped the whole repo (excluding `node_modules`) for API-key/token/private-key patterns (`AIza...`, `sk-...`, `xox[baprs]-...`, `-----BEGIN ... PRIVATE KEY-----`, and generic `key/secret/token = "<long literal>"` assignments).

All 5 matches are the same Firebase **web** config (`apiKey`, `authDomain`, `projectId`, ...) duplicated across:
- `firebase.js:8`
- `shared/lib/firebase.ts:11`
- `webapp/public/sw.js:60`
- `mobile/public/sw.js:57`
- `admin/index.html:57`

Per Firebase's own security model, web `apiKey` values are not secrets (access is enforced by Firestore/Storage rules and App Check, not by hiding the key) — **no action needed**. No private keys, bot tokens, Stripe secret keys, or service-account JSON were found hardcoded anywhere; all sensitive values (`TELEGRAM_TOKEN`, `DISCORD_BOT_TOKEN`, `BOT_SECRET`, Stripe secrets) are referenced via `process.env`, Firebase `defineSecret`/Secret Manager, or documented as CLI env vars in `functions/scripts/*.js`.

Confirmed no `.env` file is tracked in git — only `functions/discord-gateway/.env.example` is committed, and `.gitignore:28-30` excludes `.env` / `.env.*` while allowlisting `.env.example`.

### firestore.rules
Reviewed `firestore.rules` in full. No `allow read/write: if true`, no missing-auth rules. Every `users/{userId}` sub-collection scopes to `request.auth.uid == userId`; server-only fields (`plan`, `subscription`, `pointerCredits`, etc., see `protectedKeys()` at line 8) are blocked from client create/update via `hasAny(protectedKeys())` checks (lines 22, 24); `positions`, `priceAlerts`, `pointerTasks`, and `payments` correctly restrict client writes to only the fields the client should control (e.g. `positions` update at line 94-95 only allows `['exit','exitArmed']`). `config/{docId}` is fully server-only (line 101). **No changes proposed — rules look correct.**

### npm audit
- **`functions/`** (`npm audit --omit=dev`): **11 moderate**, **0 high/critical**. Not reported in detail per this run's high/critical-only threshold. (`npm audit fix` non-breaking fix is available if the owner wants to clear these opportunistically.)
- **`webapp/`** (`npm audit`): **2 high**, 0 critical:

  | Package | Installed | Severity | Issue | Fix available |
  |---|---|---|---|---|
  | `next` | 14.2.35 | high | Bundle of ~20 advisories: DoS (Image Optimizer, Server Components, Server Actions), HTTP request smuggling in rewrites, cache poisoning (Middleware/Proxy, RSC responses), SSRF (Server Actions, rewrites via attacker-controlled hostname), XSS (App Router + CSP nonces, `beforeInteractive` scripts), unauthenticated disclosure of internal Server Function endpoints | `next@16.2.12` (semver-major) |
  | `postcss` | 8.4.31 (transitive via `next`) | high | XSS via unescaped `</style>` in stringify output; arbitrary file read via attacker-controlled `sourceMappingURL` in CSS comments; path traversal in source-map auto-loading | Resolved by the same `next@16.2.12` bump |

  **Proposed fix** (not applied — flagged for the owner, this is a major-version jump requiring a regression pass, not a drop-in):
  ```diff
  # webapp/package.json
  -    "next": "14.2.35",
  +    "next": "16.2.12",
  ```
  Next 13→16 changes the app-router data-fetching defaults and drops some deprecated APIs; given `webapp/` uses `output: 'export'` (static export) and no Server Actions/Middleware today, the blast radius is likely small, but budget time to re-run `STATIC_EXPORT=1 npm run build` and smoke-test all routes after the bump, and re-check `next.config.*` for renamed options.

---

## 3. Build & Syntax Health

| Step | Result |
|---|---|
| `functions/`: `npm ci` | ✅ succeeded (340 packages) |
| `functions/`: `node --check` on all `*.js` (no `lint` script defined in `functions/package.json`) | ✅ all files pass |
| `webapp/`: `npm ci` | ✅ succeeded (113 packages) |
| `webapp/`: `STATIC_EXPORT=1 npm run build` | ✅ succeeded — 31/31 static pages generated, matches the `output: 'export'` convention wired in `webapp/next.config.*:13` |
| Root: `node --check` on all top-level `*.js` (`script.js`, `wallet.js`, `token.js`, etc.) | ✅ all files pass |

No errors to report. No fix proposals needed for this section.

---

## 4. TODO / Drift Audit

### TODO/FIXME/HACK
Searched all `*.js/.ts/.tsx/.jsx/.html/.md` (excluding `node_modules`, `.next`, `dist`, `build`) for `\b(TODO|FIXME|HACK)\b`. **Zero matches in application code.** The only hits were two lines in `.github/copilot-instructions.md` (referring to Copilot's own task-list tool, not a code TODO) — not a finding.

### Docs vs. code
- `docs/ROADMAP.md:3` already self-flags as **"SUPERSEDED as of 2026-07-24 — read this as a historical planning doc"** and points to `PRODUCT_STRATEGY.md §0` for current status. No contradiction — this is the intended pattern, not drift.
- `docs/PRODUCT_STRATEGY.md` §0 (last updated 2026-07-15) claims Position Manager, exit monitor (`processCexExits`), referral program, and copy trading are shipped. Spot-checked against `functions/`: `lib/cex-exit-monitor.js`, `lib/copytrader.js`, `lib/positions.js`, and referral logic in `lib/payments.js`/`index.js` all exist and are wired into `index.js`. **Claims match code — no drift found.**
- `README.md:39-41` calls out UI duplication across root/`mobile/`/`webapp/` as "the most common source of drift here" — consistent with this run's check #1 finding no commits to review, so no duplication check was possible this cycle. Worth double-checking on the next run with actual UI commits.
- `docs/SKILL.md`, `docs/ARBITRAGE_BOT_SKILL.md`, `docs/WALLET_INTEGRATION_SKILL.md` have no embedded dates; not checked line-by-line this run due to size (685/283/539 lines) — recommend a deeper pass if the owner wants full doc-drift coverage, since this run prioritized the dated/status docs.

No fix proposals — no contradictions found.

---

*Generated automatically by the scheduled FXcrypt1 health-check agent. No application code was modified.*
